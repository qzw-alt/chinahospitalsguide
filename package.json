{
  "name": "chinahospitalsguide",
  "version": "1.0.0",
  "description": "China Hospitals Guide - Static site built with Eleventy",
  "scripts": {
    "optimize-images": "node scripts/convert-images.js",
    "build": "npm run optimize-images && eleventy && node scripts/inject-hints.js && cleancss -o _site/styles.css _site/styles.css",
    "serve": "eleventy --serve --watch",
    "debug": "DEBUG=* eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "clean-css-cli": "^5.6.3",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=18"
  }
}
